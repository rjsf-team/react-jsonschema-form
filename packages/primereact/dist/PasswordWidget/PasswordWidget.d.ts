import { WidgetProps } from "@rjsf/core";
declare const PasswordWidget: ({ id, required, readonly, disabled, value, label, onFocus, onBlur, onChange, options, autofocus, schema, uiSchema, rawErrors, }: WidgetProps) => JSX.Element;
export default PasswordWidget;
