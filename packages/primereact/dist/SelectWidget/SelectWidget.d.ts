import { WidgetProps } from "@rjsf/core";
declare const SelectWidget: ({ schema, id, options, label, required, disabled, value, multiple, autofocus, onChange, onBlur, onFocus, placeholder, rawErrors, }: WidgetProps) => JSX.Element;
export default SelectWidget;
