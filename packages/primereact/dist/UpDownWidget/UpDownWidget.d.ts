import { WidgetProps } from "@rjsf/core";
declare const UpDownWidget: ({ id, required, readonly, disabled, label, value, onChange, onBlur, onFocus, autofocus, schema, uiSchema }: WidgetProps) => JSX.Element;
export default UpDownWidget;
