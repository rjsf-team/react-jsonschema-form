import { ComponentType } from 'react';
import { withTheme, FormProps } from '@rjsf/core';

import Theme5 from '../Theme5';

const MuiForm5: ComponentType<FormProps> = withTheme(Theme5);

export default MuiForm5;
