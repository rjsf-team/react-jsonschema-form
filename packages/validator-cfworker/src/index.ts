import customizeValidator from './customizeValidator.js';

export { customizeValidator };
export { default as CFWorkerValidator } from './validator.js';
export type * from './types.js';

export default customizeValidator();
