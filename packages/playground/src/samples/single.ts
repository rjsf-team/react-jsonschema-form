import { Sample } from './Sample';

const single: Sample = {
  schema: {
    title: 'A single-field form',
    type: 'string',
  },
  formData: 'initial value',
  uiSchema: {},
};

export default single;
