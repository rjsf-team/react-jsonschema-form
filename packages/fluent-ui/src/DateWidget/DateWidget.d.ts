import { WidgetProps } from "@rjsf/core";
declare const DateWidget: ({ id, required, readonly, disabled, label, value, onChange, onBlur, onFocus, autofocus, options, placeholder, schema, rawErrors, }: WidgetProps) => JSX.Element;
export default DateWidget;
