{
  presets: ['react'],
  env: {
    babel: {
      presets: [['env', { targets: { browsers: ['> 1%', 'last 2 versions'] } }]]
    },
    rollup: {
      presets: [['env', { modules: false }]],
      plugins: ['external-helpers']
    }
  }
}
